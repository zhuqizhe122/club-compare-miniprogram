# Fix Log — 2026-07-22 文档验收轮

本文件只记录本轮文档回归中实际发现并修正的问题，以及仍需人工确认的风险；不追溯早期开发流水账。

| # | 回归问题 | 影响 | 本轮修正 | 剩余风险 |
|---|---|---|---|---|
| 1 | `002` 仍声明“只做纯函数、不接 UI”，但当前 `list` 已同页接入四题、Top 6、搜索分类与会话态 | 规格、计划、任务状态与运行时事实冲突，无法据此验收 | 更新 `002` 的 spec、plan、tasks、quickstart，以已接入的四页实现为基线 | DevTools 中的模式切换、返回状态与小屏可读性仍待人工验收 |
| 2 | `001` 把差异摘要、预期卡和现场追问写成“后续 UI” | compare/result 已有能力在文档中被错误标成未实现 | 更新 `001` 的 spec、plan、tasks、quickstart，纳入差异高亮、倾向、中文预期卡和 3 问 | 3 个候选时的横向滚动和高亮辨识度仍待人工验收 |
| 3 | 旧验收表把 2026-07-20 的 walking skeleton 手测当作当前决策罗盘已通过 | 新增 UI 没有本轮人工证据却呈现为全绿 | 重建 `validation-checklist.md`，仅保留可复现的 Node 项已勾选，所有 DevTools 项保持未勾选 | 需要人工补日期、基础库版本和截图/文字证据 |
| 4 | README、主计划和数据契约仍只描述旧选择/倾向状态 | 使用者无法理解完整价值、双模式和会话数据边界 | 补齐四页链路、零网络/非 AI、答题/Top 6 会话态与打开/校验命令 | “零网络”仍需在 DevTools Network 面板人工复核 |
| 5 | 演示路径未固定，旧 quickstart 使用差异较弱的候选且无法展示新能力 | 2–3 分钟答辩容易漏掉 Top 6 理由、差异高亮或结果行动卡 | 新增 `DEMO-SCRIPT.md`，固定技术偏好及本地真实存在的“数字孪生协会、数学建模协会、计算机协会” | Top 6 依赖当前本地数据与确定性排序；数据变更后需同步复核脚本 |

## 本轮未改

- 未修改运行时代码、测试、脚本、路由、依赖或数据。
- 未执行微信开发者工具人工验收，因此不声称页面已经通过。

## 静态审查修复轮

| # | 回归问题 | 修正 |
|---|---|---|
| 6 | 修改问卷答案后，旧 Top 6 仍可能在页面再次 `onShow` 时恢复 | 进入“改答案”即同时清除页面与会话中的旧推荐，完成四题后才重新生成 |
| 7 | 结果页切到后台再回来会无条件清空已生成的行动卡 | `onShow` 根据当前会话倾向重建确认结果、预期卡和现场追问 |
| 8 | 关键差异摘要可能直接显示 `regular`、`project` 等内部枚举 | 比较页统一调用结构化字段中文显示函数，并增加预期卡枚举泄漏测试 |
| 9 | 极少数组合只能生成 2 条现场追问，与展示口径不一致 | 在差异/倾向问题不足时补入新人职责、真实投入或缺席弹性的通用核实问题，稳定输出 3 条 |
| 10 | 数据契约允许四个比较字段缺失，但静态校验错误地强制非空 | 校验改为接受字符串及契约允许的缺失值，页面继续统一显示“未提供” |

以上修复已通过 Node 静态校验；微信开发者工具人工项仍保持未勾选。
